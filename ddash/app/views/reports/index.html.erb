<h1> Gerrit Projects </h1>
 
<% @gerrit_projects.each do |report| %>
  <tr>
    <td><%= report %></td>
    <BR>
  </tr>
<% end %>

<h1> Jenkins Job status </h1>
<% @all_status.each do |element, value| %>
  <tr>
    <td><%= "#{element} #{value}" %></td>
    <BR>
  </tr>
<% end %>
